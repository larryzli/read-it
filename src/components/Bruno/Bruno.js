import React, { Component } from "react";
import axios from "axios";

class Bruno extends Component {
  render() {
    return (
      <div>
        <button onClick={() => console.log("HelloDarkness")} />
      </div>
    );
  }
}

export default Bruno;
